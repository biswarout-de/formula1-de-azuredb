# This is a Databricks asset bundle definition for formula1deaz.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: formula1deaz
  uuid: 73ac344f-765e-480b-868f-d7ddb04dae9e

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-890236363240393.13.azuredatabricks.net

  test:
    mode: production
    workspace:
      host: https://adb-3690747599148799.19.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/basntakumarr@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/basntakumarr@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: basntakumarr@gmail.com
        level: CAN_MANAGE

  prod:
    mode: production
    workspace:
      host: https://adb-1521820413152425.5.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/basntakumarr@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/basntakumarr@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: basntakumarr@gmail.com
        level: CAN_MANAGE
